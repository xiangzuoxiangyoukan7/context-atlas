# 文档元数据规范

核心字段和枚举以 `schemas/` 为准。所有功能和任务验收声明必须非空、唯一，并在验收矩阵中恰好出现一次。

## 核心验收结果

`not_started`、`partial`、`passed`、`not_applicable`。`passed` 必须有可定位证据和对应版本；`failed`、`blocked` 是任务或证据状态，不是验收结果枚举。

## Profile 规则

Profile 可以添加 `jdk_version`、`framework`、`package_manager` 等字段，但不能替换核心字段、删除核心验收项或改变权威来源顺序。

