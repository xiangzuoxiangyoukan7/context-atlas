# TASK-KB-012 非渐进读取工作验收

## 范围

本证据覆盖来源内嵌、初始化内容路由、目录归属与 authority 校验、显式维护边界、格式迁移和一致性资产。按用户要求，不覆盖“知识路由与渐进读取协议”。

## 已验证结果

- 格式 4 将来源类型、精确引用、观察时间、确认状态和确认时间内嵌到被支撑知识项。
- 格式 1、2、3 可生成只读迁移 Proposal；来源缺失、重复、未被引用或目标冲突时拒绝应用。
- 可选公共来源迁入 `05-知识治理/公共来源/`，不再占用项目总览，且不是理解知识项的前置条件。
- 初始化 Proposal 可表达术语、能力、功能、模块、接口、数据库、外部依赖、测试和 ADR，并写入对应固定文档。
- 空项目只生成骨架和空表，不生成虚构项目事实。
- 检查器验证固定入口、authority 目标、知识类型目录、旧结构残留及格式 4 的旧来源引用。
- 普通开发不自动调用更新 Skill、不自动创建提案文件或改写正式知识。

## 验证命令与结果

- `py -m unittest discover -s tests -q`：196 项通过。
- `py scripts/check_knowledge_base.py doc-atlas --schema-root schemas`：通过。
- `py scripts/sync_skill_assets.py --check`：通过。
- `git diff --check`：通过。

以上自动检查只证明结构、引用和确定性行为满足规则，不替代对业务内容的人工确认。
