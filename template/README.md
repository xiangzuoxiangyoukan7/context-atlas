# 新项目知识库模板

复制本目录到新项目后：

1. 修改 `knowledge-base/00-项目总览/` 中的目标、范围和边界。
2. 选择一个主 profile，可选一个或多个辅助 profile，并登记在项目总览。
3. 为真实功能创建功能卡、契约、ADR、任务包和验收项。
4. 保留 `CURRENT.md`，没有批准任务时使用“无可执行开发任务”。
5. 运行上层项目提供的 `scripts/check_knowledge_base.py`。

模板不携带任何业务完成状态。

