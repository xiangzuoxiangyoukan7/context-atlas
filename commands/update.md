---
description: 更新当前项目的 Context Atlas 知识库
---

# Context Atlas Update

执行 Context Atlas 更新流程。

要求：

1. 读取当前知识库和相关证据，生成带唯一名称的 Proposal。
2. 先展示变更目标、来源、影响、冲突和验证计划，等待用户明确确认。
3. 用户确认后，调用插件内置的结构化 `update` 执行器；不要手工修改正式知识库文件。
4. 更新后执行知识库校验，并报告实际变更文件；校验失败不得报告成功。
