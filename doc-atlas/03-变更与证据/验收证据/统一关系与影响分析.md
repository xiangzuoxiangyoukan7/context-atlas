---
id: EVID-011
type: acceptance_evidence
rel_classified_under:
  - "[[03-变更与证据/验收证据/README|IDX-EVIDENCE]]"
title: 统一关系与影响分析
---
# 统一关系与影响分析验收证据

## 验收范围

本证据覆盖历史任务 `TASK-KB-007` 的 `KB-AC-30`～`KB-AC-32`。实现提交为 `9940e02`、`b061873`、`a0b8def`、`f6f1771`、`440f022` 和 `9f12a6e`，执行日期为 2026-08-13。

## 确定性验证

| 项目 | 命令 | 退出码 | 结果 |
| --- | --- | --- | --- |
| Python 全量测试 | `py -m unittest discover -s tests -p "test_*.py"` | 0 | 126 项通过 |
| Python 注释与类型 | `py scripts/check_python_documentation.py` | 0 | 通过 |
| 规则覆盖 | `py scripts/check_rule_coverage.py --root .` | 0 | 通过 |
| Skill 资产同步 | `py scripts/sync_skill_assets.py --check` | 0 | 通过 |
| 单技术栈样例 | `py scripts/check_knowledge_base.py examples/single-stack --schema-root schemas` | 0 | 通过 |
| 多技术栈样例 | `py scripts/check_knowledge_base.py examples/multi-stack --schema-root schemas` | 0 | 通过 |
| 差异格式 | `git diff --check` | 0 | 通过 |

## 代表性影响结果

对多技术栈样例执行：

```powershell
py scripts/analyze_knowledge_impact.py examples/multi-stack --schema-root schemas --changed-id TABLE-KNOWLEDGE-001 --change-type field_removed --format json
```

命令退出码为 0，产生以下脱敏结构结果：

| 变化项 | 受影响项 | 关系 | 等级 | 深度 |
| --- | --- | --- | --- | --- |
| `TABLE-KNOWLEDGE-001` | `MODULE-QUERY-001` | `rel_reads` | `required` | 1 |
| `TABLE-KNOWLEDGE-001` | `F01` | `rel_depends_on` | `review_required` | 2 |

这证明删除表字段可被识别为模块直接必改，同时功能的二级不确定影响不会被自动升级为必改。命令只读取文件；集成测试比较运行前后字节，确认知识文件没有变化。

## 验收项映射

| 验收编号 | 证据 | 结果 |
| --- | --- | --- |
| `KB-AC-30` | 受控关系目录、Wikilink 解析、七类精确错误码、三个端到端反例 | passed |
| `KB-AC-31` | 正反向索引、直接/间接传播、三级等级、未知变化降级、只读 CLI | passed |
| `KB-AC-32` | 两条权威规则、关系与影响模板、Skill 渐进说明、两套可计算黄金样例、自包含检查器 | passed |

## 证据边界

自动验证只证明结构、关系和规则计算符合契约，不证明项目业务内容真实或完整。`required` 仍需责任人确认处理方案；`review_required` 必须人工判断；`informational` 也不能替代必要的文件链接维护。分析结果不决定任何开发任务能否执行。
